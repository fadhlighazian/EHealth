<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.umn.ehealth">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AjukanActivity" />
        <activity android:name=".LamaWaktuActivity" />
        <activity android:name=".CatatanPasienActivity" />
        <activity android:name=".AntrianActivity" />
        <activity android:name=".PesanObatActivity" />
        <activity android:name=".GetAntrian" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".RiwayatActivity"></activity>
    </application>

</manifest>